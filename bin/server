#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative "../lib/expense_splitter"

port = Integer(ENV.fetch("PORT", "4567"))
puts "expense-splitter listening on http://localhost:#{port} (Ctrl-C to stop)"
ExpenseSplitter::API::Server.new(port: port).start
